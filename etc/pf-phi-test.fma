@!prae{
  var @if = 0;

  var @ifElse = 0;

  if (@ifElse < 2) {
    @ifElse = 1;
    @ifElse += 3;
  };
  else {
    @ifElse = 7;
  };

  if (@ifElse < 2) {
    @ifElse = 1;
    @ifElse += 3;
  };
  else {
    @ifElse = 7;
  };

  var @while = 0;


@}